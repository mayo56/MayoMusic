import React from 'react'

function ComingSoon(): React.JSX.Element {
  return (
    <div style={{ width: '100%', height: 'calc(100vh - 30px)' }} className={'incoming_feature'} />
  )
}

export default ComingSoon
